import { MarketNamesPipe } from './market-names.pipe';

describe('MarketNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new MarketNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
